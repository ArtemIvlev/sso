title: card_number_mb
description: Вход по номеру банковской карты без пароля
allOf:
  - type: object
    properties:
      cardNumber:
        type: string
        description: номер карты 
      password:
        type: string
        description: пароль
    required:
      - cardNumber

  - $ref: ./grantDefault.yaml
  - $ref: ./secondFactor.yaml
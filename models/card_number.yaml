title: card_number
description: card_number - Вход по номеру банковской карты и паролю в ИБ
allOf:
  - $ref: ./card_number_mb.yaml
required:
  - password
x-examples:
  Example (1FA):
    cardNumber: string
    password: string
    scope: openid
    grant_type: card_number_mb
  Example (2FA):
    cardNumber: string
    password: string
    sessionDataKey: string
    scope: openid
    grant_type: card_number_mb
    transactionId: string
    otp: string

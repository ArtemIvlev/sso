title: password
description: пароль
allOf:
  - properties:
      password:
        type: string
        description: пароль
      password_must_change:
        type: boolean
      password_expiration_time:
        type: string
        format: date-time
      password_policy:
        type: string
      password_failure:
        type: string
        format: date-time
type: object

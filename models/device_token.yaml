title: passcode
description: вход по пасскоду в МП
allOf:
  - type: object
    properties:
      secureCode:
        description: используется для входа по пасскоду
        type: string
      challenge:
        description: используется для входа по пасскоду
        type: string
      deviceTokenID:
        description: используется для входа по пасскоду
        type: string
  - $ref: ./secondFactor.yaml
  - $ref: ./grantDefault.yaml
required:
  - secureCode
  - challenge
  - deviceTokenID
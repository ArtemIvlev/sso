title: login_mb
description: Вход по логину или унк без пароля в МП
allOf:
  - type: object
    properties:
      login:
        type: string
        description: УНК или Логин (Альяс)
      password:
        type: string
        description: пароль
    required:
      - login
  - $ref: ./grantDefault.yaml
  - $ref: ./secondFactor.yaml
